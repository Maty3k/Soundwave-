Icon sources. `icon.svg` is the standard icon (same artwork as `public/favicon.svg`);
`icon-maskable.svg` is full-bleed with the glyph inside the maskable safe zone.
The PNGs in `public/icons/` were rendered once from these with headless Chrome screenshots
(`--window-size=<n>,<n> --screenshot=...`).
